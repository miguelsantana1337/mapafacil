{
  "name": "mapafacil",
  "version": "0.1.0",
  "private": true,
  "description": "Workspace visual de estratégia e mapas mentais.",
  "scripts": {
    "dev": "python3 -m http.server 8743",
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
